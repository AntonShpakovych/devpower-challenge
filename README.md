# devpower-challenge

```shell
git clone https://github.com/AntonShpakovych/devpower-challenge.git
сd devpower-challenge
create .env (example data in env.example)
docker-compose up --build get_data
docker-compose up --build print_data
```
